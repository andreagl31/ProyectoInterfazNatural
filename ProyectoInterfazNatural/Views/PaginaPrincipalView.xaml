<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoInterfazNatural.Views.PaginaPrincipalView"
             Title="PaginaPrincipalView">
    <!--Grid principal-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--Grid banner-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <BoxView Color="Black" HeightRequest="2" Grid.ColumnSpan="2" Grid.Row="1" />
            <Label Text="{Binding UsuarioActualApp.Username,
                StringFormat='Biblioteca de {0}'}" FontSize="24" FontAttributes="Bold" Grid.Row="0" Margin="10"/>
            <Image Source="red.png" WidthRequest="40" HeightRequest="40" Grid.Column="1" Grid.Row="0" HorizontalOptions="End" Margin="10"/>
        </Grid>
        <!--Grid libros y botón-->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <CollectionView ItemsSource="{Binding UsuarioActualApp.myBooks}" HeightRequest="200" Margin="0,10,0,10">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightGray" CornerRadius="10" Padding="5">
                            <VerticalStackLayout>
                                <Image Source="{Binding ImageSource}" WidthRequest="100" HeightRequest="150"/>
                                <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button Text="Di el nombre del libro" Command="{Binding BuscarLibroCommand}"  Grid.Row="1" HorizontalOptions="Center" VerticalOptions="End" Margin="0,0,0,0" />
        </Grid>
        <!--Footer-->
        <Grid Grid.Row="2" VerticalOptions="End">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <BoxView Color="Black" HeightRequest="2" Grid.ColumnSpan="2" Grid.Row="0" />
            <HorizontalStackLayout HorizontalOptions="Center" VerticalOptions="End" Grid.Row="1" >
                <Image Source="casa.png" WidthRequest="50" HeightRequest="50" Margin="10"/>


                <Image Source="busqueda.png" WidthRequest="50" HeightRequest="50" Margin="10">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnBusquedaTapped" NumberOfTapsRequired="1" />
                    </Image.GestureRecognizers>
                </Image>

            </HorizontalStackLayout>
        </Grid>
       
    </Grid>
</ContentPage>